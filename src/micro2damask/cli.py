from .config import Config
from .pipeline import run_pipeline

def main():
    raise SystemExit("Create a Config object in Python for now; CLI configuration can be added next.")

if __name__ == "__main__":
    main()
